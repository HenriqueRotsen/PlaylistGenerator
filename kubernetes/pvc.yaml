apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: default-storage-class-henriqueferreira
  namespace: henriqueferreira
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      namespace: henriqueferreira